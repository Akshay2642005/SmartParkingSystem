session:
  current_goal: Complete M2 — Sensor Layer verification and firmware hardening.
  current_phase: M2
  completed:
    - M0 — Specification baseline
    - M1 — ESP32 Foundation (firmware boots, clean build, Wokwi)
    - Ultrasonic (HC-SR04) sensor driver
    - Parking state machine (UNKNOWN/FREE/OCCUPIED/ERROR)
    - Slot configuration (3 slots, per-slot GPIO + thresholds)
  in_progress:
    - M2 — Sensor Layer verification
    - Firmware code-quality review and fixes
  next:
    - Verify sensor integration in Wokwi
    - Decide device communication protocol (ADR-0005)
    - Begin M3 — Device Platform
  blockers:
    - Communication protocol pending
    - Backend framework / persistence pending
    - Real-time update mechanism pending
    - Device identity scheme pending
  decisions_pending:
    - ADR-0005 Device Communication
    - ADR-0006 Backend
    - ADR-0008 Device Identity
    - ADR-0009 Real-Time Updates