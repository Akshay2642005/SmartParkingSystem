session:
  current_goal: M4 — Backend (template imports P1-P7, persistence decision
    via ADR-0006).
  current_phase: M4
  completed:
    - M0 — Specification baseline
    - M1 — ESP32 Foundation (firmware boots, clean build, Wokwi)
    - Ultrasonic (HC-SR04) sensor driver
    - Parking state machine (FREE/OCCUPIED/ERROR) with hysteresis
    - State-change events (OCCUPIED/FREED)
    - Parking lot model + statistics (total/occupied/available)
    - parking_lot_scan() API (polling moved out of main.c)
    - Slot configuration (3 slots, per-slot GPIO + thresholds)
    - Phase 3 — Ultrasonic sensor abstraction (GPIO validation, measurement
      range 2-400 cm, named constants, non-fatal error returns)
    - Phase 4 — Sensor measurement validation (invalid readings -> ERROR,
      never silently FREE/OCCUPIED)
    - Phase 5 — Parking slot state machine (deterministic ERROR recovery,
      pre-error state restore, no spurious events)
    - Phase 6 — Event model (events identify the slot and the measurement
      that caused the change)
    - Phase 7 — Event handling separation (handle_parking_event as the single
      consumer seam; state machine does not log)
    - Phase 8 — Lot statistics (error_count added, total = occupied + available
      + error invariant)
    - Phase 9 — Lot query API audit (four const-correct getters, query block
      documented)
    - Phase 10 — Slot query API (const read accessors for slot state and
      distance)
    - Phase 11 — Measurement stability (per-slot EMA filter, seeded on first
      valid reading; invalid readings bypass the filter)
    - Phase 12 — Occupancy debouncing (configurable N-consecutive confirmation,
      band readings reset counters, recovery stays immediate)
    - Phase 13 — Sensor polling timing (PARKING_SCAN_INTERVAL_MS constant,
      vTaskDelayUntil absolute scheduling)
    - Phase 14 — Separate parking task (dedicated xTaskCreate'd scan task,
      named stack size / priority constants; app_main returns after setup)
    - Near-floor tolerance fix (ultrasonic_distance_is_plausible shared by
      driver and parking validator; absorbs us-timing jitter at the 2 cm floor)
    - Phase 15 — Error handling strategy (policy documented in
      firmware-architecture § Error Handling Policy; trigger-side GPIO
      failures surface as recoverable ESP_ERR_INVALID_STATE)
    - Phase 16 — Logging convention (level table documented in
      firmware-architecture § Logging; WARN on ERROR recovery)
    - Phase 17 — Configuration cleanup (parking_settings.h separates hardware
      algorithm knobs, single definition site per constant)
    - Phase 18 — Static memory review (audit table with evidence in
      firmware-architecture; 81 percent stack headroom measured in Wokwi)
    - Phase 19 — Wokwi simulation improvements (wiring audit, debug injection
      hook gated by CONFIG_PARKING_DEBUG_INJECT, headless matrix scenario;
      sensor-integration verification PASS)
    - Phase 20 — Automated local testing (CMake/CTest host units over the
      pure parking domain with stubbed IDF surface; 27 cases passing;
      Redis-style [ok] output; make test runner in firmware/esp32 Makefile).
      Portable case sources in main/test/ also compile into the firmware;
      `selftest` serial command runs them on-target under FreeRTOS and the
      headless Wokwi scenario confirms the SELFTEST PASSED verdict (20 cases)
    - Phase 21 — MQTT payload builder (pure protocol v1 JSON encoder in
      main/mqtt, host-tested as unit/payload with a byte-exact golden
      snapshot, lowercase wire vocabulary translated from the serial
      vocabulary via an exhaustive switch; 33 host cases green). Wire
      contract state list corrected to free|occupied|error in
      architecture/communication.md.
    - Phase 22 — MQTT client integration (net/wifi_sta.c blocking connect +
      mqtt/mqtt_publisher.c task over espressif/mqtt 1.1.0; retained
      online/offline LWT lifecycle, transition snapshots + 30 s refresh,
      QoS 1 no send-retries per contract; Kconfig-gated, =n build clean at
      0x2bba0 with publisher compiled out). E2E verified in Wokwi through a
      local wokwigw bridge to broker.emqx.io - every scenario transition seen
      as a seq-incrementing retained snapshot; teardown LWT offline observed.
      Key lessons recorded in docs/impl/firmware/PHASE-22 (esp_netif handle
      vs esp_err_t; IDF requires explicit esp_wifi_connect on STA_START;
      wokwigw enables hardware-free MQTT testing).
    - M2 close-out — PROMPT.md completion-criteria audit PASS (2026-08-25).
      All 17 PROMPT.md local-subsystem criteria mapped to implementation,
      test, and doc evidence in
      docs/specs/quality/verifications/m2-completion-criteria.md. The M2
      milestone gate is closed; networking work (Phases 21-22) proceeded
      only after the local subsystem criteria were met.
    - Phase 23 — Backend MQTT ingest (server/parking-server - rumqttc
      subscriber, validation into per-(site,section) store, axum
      /internal/state debug endpoint). Review corrections recorded in
      docs/impl/server/PHASE-23 - slot count learned not hardcoded,
      unknown removed from SlotState, slots[].id/changed_ms required,
      retained offline resets the stale baseline (contract amendment in
      communication.md § Message Ordering), status topics dispatched not
      warn-rejected, serde syntax-vs-schema classification. Gates - 14/14
      cargo tests, clippy/fmt clean, firmware build + 34/34 host cases.
      Live drill PASS - verifications/device-backend.md.
    - Contract alignment pass (2026-08-25, post Phase 23) - spec brought to
      implemented truth before further work. Slots-per-section declared a
      deployment property (diagram, example payload, completeness rule),
      v1 reconnect policy corrected from planned exponential backoff to the
      shipped fixed-interval retries with backoff explicitly deferred,
      and a new communication.md § Backend Ingest Rules section pinning
      consumer obligations (identity from topic, reject taxonomy, session
      reset, learned slot count, server_ts stamping). ADR-0005 validation
      status annotated (store half PASS; WebSocket half pending M5).
    - Backend reference template staged (Phase 24 setup) - rust-starter
      cloned to server/my-api as a local-only baseline, excluded via
      .git/info/exclude; feature inventory and phased import plan (P1
      errors, P2 config, P3 middleware, P4 telemetry, P5 SeaORM+Postgres,
      P6 OpenAPI, P7 hardening) recorded in impl PHASE-24. ADR-0006 updated
      to decided-by-implementation truth (axum/tokio/rumqttc) with
      persistence left Candidate-Pending; tech-stack rows current;
      PLAN M4 deliverables enumerate the import steps; audit record in
      quality/audits/backend-reference-adoption.md.
    - (2026-08-26) Full migration phase series authored in impl
      PHASE-25..33 (local-only): P1 errors/envelope, P2 layered config,
      P3 HTTP hardening + health endpoints, P4 telemetry module,
      P5 SeaORM persistence (GATED on ADR-0006 acceptance), P6 OpenAPI +
      read API, P7 WebSocket fan-out per ADR-0009, P8 ops hardening
      (cargo-make/deny/rate-limit/metrics), P9 auth (GATED until a second
      actor exists). Each doc carries tailored code + explanation + exact
      Cargo deltas. Per user directive no server code written yet;
      implementation starts phase-by-phase on explicit go-ahead.
    - P2 configuration crate landed (2026-08-26) — workspace crate with
      layered YAML/env config, serde-defaulted schema (PrimaryConfig,
      ServerConfig, MQTTConfig), startup validation, config.yml committed.
      Old hand-rolled Config::from_env deleted; main.rs/mqtt.rs adapted.
    - P4 structured telemetry crate landed (2026-08-26) — workspace crate
      with tracing-subscriber (EnvFilter, compact/pretty/json, ChronoUtc),
      tracing-error ErrorLayer, LogFormat enum with PartialEq, Telemetry
      guard struct; 3 error tests + 5 config schema tests. Observability
      row in tech-stack.md flipped to Current; Observability subsection
      added to backend-architecture.md.

  in_progress:
    - M4 imports from server/my-api reference - P2 configuration and P4
      telemetry crates landed; next P3 HTTP hardening + health endpoints

  decisions:
    - Decision (2026-08-23) — device-backend protocol, ADR-0005 accepted
      (MQTT with a site-local Mosquitto broker acting as coordinator; nodes
      publish retained QoS 1 section snapshots; LWT for node liveness; the
      Rust backend subscribes via rumqttc; a dedicated gateway service is
      deliberately deferred). ADR-0009 accepted (backend-to-dashboard
      WebSocket using snapshot-then-deltas with a reserved command envelope).
      Wire contract (topics, payload schema v1, seq/idempotency, reconnect
      policy) defined in architecture/communication.md.
      Evidence at docs/specs/architecture/communication.md

  next:
    - M4 — Device Platform close-out (broker bring-up done via local
      mosquitto + public EMQX; firmware MQTT publisher, payload builder,
      backend ingest and reconnect policy all landed in Phases 21-23;
      remaining work is per-node credentials once a second node exists,
      watchdog/recovery review)
    - M4 imports P3-P7 from server/my-api reference (HTTP hardening +
      health endpoints, persistence pending ADR-0006 acceptance + Postgres,
      OpenAPI, WebSocket fan-out, rate-limit/metrics)
    - Device identity scheme (ADR-0008)
    - Backend framework and persistence (ADR-0006)
  blockers:
    - Backend framework / persistence pending
    - Device identity scheme pending
  decisions_pending:
    - ADR-0006 Backend
    - ADR-0008 Device Identity
